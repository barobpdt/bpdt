import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: "center"
    },
    title: {
        fontSize: 28,
        fontWeight: "bold",
        color: "red",
        marginTop: 10,
        marginBottom: 10
    }
})