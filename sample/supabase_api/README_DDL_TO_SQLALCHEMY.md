# DDL to SQLAlchemy Model Generator

DDL 스키마를 파싱하여 SQLAlchemy 모델을 자동으로 생성하는 도구들입니다.

## 📋 개요

이 프로젝트는 다음과 같은 방법들로 SQLAlchemy 모델을 생성할 수 있습니다:

1. **커스텀 DDL 파서** (`ddl_to_sqlalchemy.py`) - DDL 파일을 직접 파싱
2. **sqlacodegen 래퍼** (`sqlacodegen_wrapper.py`) - 기존 데이터베이스에서 모델 생성
3. **다양한 데이터베이스 지원** - MySQL, PostgreSQL, SQLite, Oracle, MSSQL

## 🚀 설치

### 1. 기본 의존성 설치

```bash
pip install sqlalchemy
```

### 2. sqlacodegen 설치 (선택사항)

```bash
# 방법 1: 직접 설치
pip install sqlacodegen

# 방법 2: 래퍼 스크립트 사용
python sqlacodegen_wrapper.py --install
```

## 📖 사용법

### 방법 1: 커스텀 DDL 파서 사용

#### 기본 사용법

```bash
# DDL 파일에서 모델 생성
python ddl_to_sqlalchemy.py example_ddl.sql models.py

# 출력 파일 없이 콘솔에 출력
python ddl_to_sqlalchemy.py example_ddl.sql
```

#### 예제 실행

```bash
# 예제 DDL 파일로 모델 생성
python ddl_to_sqlalchemy.py example_ddl.sql generated_models.py
```

### 방법 2: sqlacodegen 래퍼 사용

#### 기존 데이터베이스에서 모델 생성

```bash
# MySQL 데이터베이스
python sqlacodegen_wrapper.py \
    --database "mysql+pymysql://user:password@localhost/dbname" \
    --output models.py

# PostgreSQL 데이터베이스
python sqlacodegen_wrapper.py \
    --database "postgresql://user:password@localhost/dbname" \
    --output models.py

# SQLite 데이터베이스
python sqlacodegen_wrapper.py \
    --database "sqlite:///database.db" \
    --output models.py
```

#### DDL 파일에서 모델 생성

```bash
python sqlacodegen_wrapper.py \
    --ddl example_ddl.sql \
    --output models.py
```

#### 고급 옵션

```bash
# 특정 테이블만 생성
python sqlacodegen_wrapper.py \
    --database "mysql+pymysql://user:password@localhost/dbname" \
    --tables "users,products,orders" \
    --output models.py

# 뷰, 인덱스, 제약조건 포함
python sqlacodegen_wrapper.py \
    --database "mysql+pymysql://user:password@localhost/dbname" \
    --include-views \
    --include-indexes \
    --include-constraints \
    --output models.py
```

## 🔧 지원하는 기능

### 커스텀 DDL 파서 (`ddl_to_sqlalchemy.py`)

#### 지원하는 SQL 타입
- **정수형**: `INT`, `INTEGER`, `BIGINT`, `SMALLINT`
- **실수형**: `FLOAT`, `DOUBLE`, `DECIMAL`, `NUMERIC`
- **문자형**: `VARCHAR`, `CHAR`, `TEXT`
- **날짜형**: `DATE`, `DATETIME`, `TIMESTAMP`
- **기타**: `BOOLEAN`, `JSON`, `BLOB`

#### 지원하는 제약조건
- `PRIMARY KEY`
- `FOREIGN KEY`
- `UNIQUE`
- `NOT NULL`
- `DEFAULT`
- `AUTO_INCREMENT`
- `COMMENT`

#### 지원하는 구조
- 테이블 정의
- 컬럼 정의
- 외래키 관계
- 인덱스
- 주석

### sqlacodegen 래퍼 (`sqlacodegen_wrapper.py`)

#### 지원하는 데이터베이스
- MySQL
- PostgreSQL
- SQLite
- Oracle
- Microsoft SQL Server

#### 옵션들
- `--tables`: 특정 테이블만 생성
- `--include-views`: 뷰 포함
- `--include-indexes`: 인덱스 포함
- `--include-constraints`: 제약조건 포함
- `--include-joined`: 조인된 테이블 포함
- `--include-inflect`: 단수형 변환 포함

## 📝 생성되는 모델 예시

### 입력 DDL (example_ddl.sql)
```sql
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) NOT NULL UNIQUE COMMENT '사용자명',
    email VARCHAR(100) NOT NULL UNIQUE COMMENT '이메일',
    password_hash VARCHAR(255) NOT NULL COMMENT '비밀번호 해시',
    is_active BOOLEAN DEFAULT TRUE COMMENT '활성 상태',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '생성일시'
);
```

### 생성되는 SQLAlchemy 모델
```python
"""
Auto-generated SQLAlchemy models from DDL
Generated by DDL to SQLAlchemy Generator
"""

from sqlalchemy import Column, Integer, String, Text, Boolean, Date, DateTime, Numeric, BigInteger, SmallInteger, Float, JSON, LargeBinary, ForeignKey
from sqlalchemy.ext.declarative import declarative_base
from sqlalchemy.orm import relationship
from datetime import datetime

Base = declarative_base()

class Users(Base):
    __tablename__ = "users"

    # 사용자명
    id = Column(Integer(), primary_key=True, autoincrement=True)
    # 사용자명
    username = Column(String(50), nullable=False, unique=True)
    # 이메일
    email = Column(String(100), nullable=False, unique=True)
    # 비밀번호 해시
    password_hash = Column(String(255), nullable=False)
    # 활성 상태
    is_active = Column(Boolean(), default=True)
    # 생성일시
    created_at = Column(DateTime(), default=datetime.now)

    def __repr__(self):
        return f"<Users(id={self.id})>"
```

## 🛠️ 고급 사용법

### 1. 관계 설정 자동 생성

외래키가 있는 테이블의 경우 관계 설정이 자동으로 생성됩니다:

```python
class OrderItems(Base):
    __tablename__ = "order_items"

    id = Column(Integer(), primary_key=True, autoincrement=True)
    order_id = Column(Integer(), ForeignKey('orders.id'), nullable=False)
    product_id = Column(Integer(), ForeignKey('products.id'), nullable=False)
    quantity = Column(Integer(), nullable=False)
    unit_price = Column(Numeric(10, 2), nullable=False)
    total_price = Column(Numeric(10, 2), nullable=False)

    # 자동 생성된 관계
    orders = relationship("Orders", back_populates="order_items")
    products = relationship("Products", back_populates="order_items")
```

### 2. 커스텀 타입 매핑

필요에 따라 타입 매핑을 수정할 수 있습니다:

```python
# ddl_to_sqlalchemy.py의 _parse_type 메서드 수정
def _parse_type(self, type_str: str) -> Tuple[str, Optional[int], Optional[int], Optional[int]]:
    type_str = type_str.upper()
    
    # 커스텀 타입 매핑 추가
    if 'UUID' in type_str:
        return 'String(36)', 36, None, None  # UUID를 String(36)으로 매핑
    
    # 기존 로직...
```

### 3. 모델 후처리

생성된 모델에 추가 기능을 넣을 수 있습니다:

```python
# 생성된 모델에 메서드 추가
class Users(Base):
    __tablename__ = "users"
    
    # ... 기존 컬럼들 ...
    
    @property
    def full_name(self):
        return f"{self.first_name} {self.last_name}"
    
    def is_admin(self):
        return any(role.name == 'admin' for role in self.roles)
```

## 🔍 문제 해결

### 일반적인 문제들

#### 1. DDL 파싱 오류
```
Error: No tables found in DDL file
```
- DDL 파일의 문법을 확인하세요
- CREATE TABLE 문이 올바른 형식인지 확인하세요

#### 2. 데이터베이스 연결 오류
```
Error: (pymysql.err.OperationalError) (2003, "Can't connect to MySQL server")
```
- 데이터베이스 서버가 실행 중인지 확인하세요
- 연결 정보(호스트, 포트, 사용자명, 비밀번호)를 확인하세요

#### 3. 권한 오류
```
Error: (pymysql.err.OperationalError) (1142, "SELECT command denied")
```
- 데이터베이스 사용자에게 적절한 권한을 부여하세요

### 디버깅 팁

1. **상세 로그 확인**
   ```bash
   python ddl_to_sqlalchemy.py example_ddl.sql 2>&1 | tee debug.log
   ```

2. **DDL 문법 검증**
   ```bash
   # MySQL에서 DDL 문법 검증
   mysql -u root -p -e "SOURCE example_ddl.sql;"
   ```

3. **생성된 모델 검증**
   ```python
   from generated_models import Base
   from sqlalchemy import create_engine
   
   engine = create_engine('sqlite:///:memory:')
   Base.metadata.create_all(engine)
   ```

## 📚 추가 리소스

### 관련 도구들
- [sqlacodegen](https://github.com/agronholm/sqlacodegen) - 공식 sqlacodegen
- [alembic](https://alembic.sqlalchemy.org/) - 데이터베이스 마이그레이션
- [SQLAlchemy](https://www.sqlalchemy.org/) - 공식 문서

### 유용한 링크
- [SQLAlchemy 모델 정의 가이드](https://docs.sqlalchemy.org/en/14/orm/basic_relationships.html)
- [데이터베이스 스키마 설계 모범 사례](https://www.sqlstyle.guide/)

## 🤝 기여하기

버그 리포트나 기능 요청은 이슈를 통해 제출해주세요.

### 개발 환경 설정
```bash
# 저장소 클론
git clone <repository-url>
cd ddl-to-sqlalchemy-generator

# 가상환경 생성
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt

# 테스트 실행
python -m pytest tests/
```

## 📄 라이선스

이 프로젝트는 MIT 라이선스 하에 배포됩니다. 